#include <stdio.h>
#include <stdlib.h>

int main()
{
    // let's say we have a variable of type int
    int x;
    // then we want to take the user input for this variable using scanf()
    scanf("%d",&x);
    // and then print the variable
    printf("%d",x);
}
